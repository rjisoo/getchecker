<h1>GET Request Received</h1>

<table>
	{{#if paramList.length}}
		<h2>Parameters</h2>
		<tr>
			<th>Name</th>
			<th>Value</th>
		</tr>
	{{/if}}
	{{#each paramList}}
		<tr>
			<td>{{this.name}}</td>
			<td>{{this.value}}</td>
		</tr>
	{{/each}}
</table>